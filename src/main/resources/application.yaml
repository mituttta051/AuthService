spring:
  application:
    name: AuthService
  datasource:
    url: jdbc:postgresql://localhost:5432/authdb
    username: admin
    password: secret
  jpa:
    hibernate:
      ddl-auto: update
  web:
    resources:
      add-mappings: false